spring.application.name=PlanningProject
spring.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/PlanningProjectDB
spring.datasource.username=postgres
spring.datasource.password=1111

minio.endpoint=${MINIO_ENDPOINT:http://localhost:9000}
minio.access-key=minioadmin
minio.secret-key=minioadmin
minio.bucket=avatars

spring.kafka.bootstrap-servers=localhost:29092

spring.jpa.hibernate.ddl-auto=create

token.signing.key=basebasebasebasebasebasebasebasebasebasebasebasebasebasebasebase

service.name=plannerBackend